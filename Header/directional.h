#include "inputRead.h"
#include "stdbool.h"
#include "vectors.h"

uint8_t turnVAL();

void rotVAL(int32_t turnVal);

int32_t dirVAL();

void directionVector(int32_t dVAL, VECTOR *v);

void rawDirection();

/*
void direction(VECTOR *v){
    uint8_t nDIR;
    nDIR = nDIR + turnVAL();
    if tVAL(tVAL < 16){
        tVAL = 0;
    } else {
    switch()
    case 0:
    }
}
*/
